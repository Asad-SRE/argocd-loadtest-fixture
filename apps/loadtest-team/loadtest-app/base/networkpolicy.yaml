apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: loadtest-fixture
spec:
  podSelector:
    matchLabels:
      app: loadtest-fixture
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: loadtest-fixture
